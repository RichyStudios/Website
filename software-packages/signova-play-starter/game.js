const canvas=document.querySelector('#game'),ctx=canvas.getContext('2d'),status=document.querySelector('#status');let player={x:80,y:190},goal={x:520,y:100},score=0;function resetGoal(){goal={x:60+Math.random()*520,y:60+Math.random()*260}}function move(key){const step=18;if(key==='ArrowLeft'||key==='a')player.x-=step;if(key==='ArrowRight'||key==='d')player.x+=step;if(key==='ArrowUp'||key==='w')player.y-=step;if(key==='ArrowDown'||key==='s')player.y+=step;player.x=Math.max(18,Math.min(622,player.x));player.y=Math.max(18,Math.min(362,player.y));if(Math.hypot(player.x-goal.x,player.y-goal.y)<35){score++;status.textContent=`Connections: ${score}`;resetGoal()}draw()}function draw(){ctx.clearRect(0,0,640,380);ctx.strokeStyle='#27569c';for(let x=0;x<640;x+=40){ctx.beginPath();ctx.moveTo(x,0);ctx.lineTo(x,380);ctx.stroke()}for(let y=0;y<380;y+=40){ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(640,y);ctx.stroke()}ctx.fillStyle='#79efd2';ctx.beginPath();ctx.arc(goal.x,goal.y,22,0,Math.PI*2);ctx.fill();ctx.fillStyle='#fff';ctx.beginPath();ctx.arc(player.x,player.y,15,0,Math.PI*2);ctx.fill()}addEventListener('keydown',e=>move(e.key));document.querySelectorAll('button').forEach(b=>b.onclick=()=>move(b.dataset.key));draw();
